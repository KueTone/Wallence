import { Text, View } from 'react-native';

export const AddExpense = () => {
          return (
                    <View>
                              <Text>Add Expenses</Text>
                    </View>
          );
};