import { Text, View } from "react-native";

export const Settings = () => {
          return (
                    <View>
                              <Text>Hello, Settings!</Text>
                    </View>
          );
};
